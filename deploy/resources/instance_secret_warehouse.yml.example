# Example: warehouse credentials for Vulcan (Snowflake or Postgres)
# Copy to instance_secret_warehouse.yml, fill values, then:
#   dataos-ctl apply -f deploy/resources/instance_secret_warehouse.yml -w ct-sandbox
#
# Rename resource id in bundle.yml dependency from warehouse-secret → match name below.

name: sales-workforce-warehouse-secret
version: v1
type: instance-secret
tags:
  - sales-workforce
  - vulcan
description: Warehouse credentials for Sales & Workforce Vulcan project (Pacific / ct-sandbox)
layer: user
owner: jitendrakumartmdcio

instance-secret:
  type: key-value-properties
  acl: rw
  data:
    # Snowflake example — use env-specific keys your platform expects
    SNOWFLAKE_ACCOUNT: YOUR_ACCOUNT
    SNOWFLAKE_USER: YOUR_USER
    SNOWFLAKE_PASSWORD: YOUR_PASSWORD
    SNOWFLAKE_WAREHOUSE: YOUR_WH
    SNOWFLAKE_DATABASE: YOUR_DB
    SNOWFLAKE_ROLE: YOUR_ROLE

    # Postgres example (local / dev)
    # POSTGRES_HOST: warehouse
    # POSTGRES_PORT: "5432"
    # POSTGRES_DB: warehouse
    # POSTGRES_USER: vulcan
    # POSTGRES_PASSWORD: vulcan
